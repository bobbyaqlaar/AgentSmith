# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

DemoScript.md

# Generated by scripts/generate-ide-config.py from templates/agent-rules.yaml.
# The framework does not track its own IDE configs — a tenant generates them in
# ITS repo. Listed because running the repo's own tool here writes six untracked
# files into the tree, and `git add -A` would commit them.
.cursorrules
CLAUDE.md
AGENTS.md
GEMINI.md
.github/copilot-instructions.md
.agents/
.agent-history.log

# Ops Portal (Next.js)
portal/node_modules/
portal/.next/
portal/.env.local
portal/.env*.local
portal/*.tsbuildinfo
portal/next-env.d.ts

# In-App Widget
templates/in-app-widget/node_modules/

# Runtime local blobs (dev)
runtime/.hitl_blobs/

# circuit_breaker.py's local dev-session state cache
.agent-rfc/fixtures/token_velocity_cache.json
.agent-rfc/fixtures/eval_results.json
.agent-rfc/fixtures/fairness_eval_results.json
.agent-rfc/fixtures/adversarial_eval_results.json
.agent-rfc/fixtures/hallucination_eval_results.json
.agent-rfc/fixtures/rag_poison_eval_results.json
.agent-rfc/fixtures/delivery_evidence.json
.agent-rfc/fixtures/delivery_evidence.md

# Git worktrees (local isolation)
.worktrees/

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp
# Claude local (per-machine) settings — .claude/settings.json stays tracked
.claude/settings.local.json

# Local secrets
.env
.env.local
docker-compose.override.yml

# Obsidian vault config (personal editor state)
.obsidian/

# Framework repo's own event log (generated)
.agent-history.log
security-evidence/
