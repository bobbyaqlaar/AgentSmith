# Tool allowlist (SEC-TOOL-001). Deny-by-default in strict mode.
# Copy to .agent-rfc/security/tool_allowlist.yaml
version: 1
tools:
  - name: example_tool
    allowed: true
    notes: Replace with tenant-registered tool names.
